import { environment } from "src/environments/environment";

export const applicationApis = {
  tradeServeillanceUrl: `${environment.baseUrl}api/Trade`,
  orderServeillanceUrl: `${environment.baseUrl}api/Order`,
};
